#:import os os
#:import Settings settings.settings

<CustomScreen>
	canvas:
		Color:
			rgba: (1, 1, 1, 1)
		Rectangle:
			size: self.size
			pos: self.pos
			source: os.path.join(Settings.IMAGES_DIR, 'bg.png')